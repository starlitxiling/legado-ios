# 所有 markdown 一律不过 prettier —— 症结是它对表格的处理。
#
# prettier 把表格列按最宽单元格补空格对齐，且分隔行 `| --- |` 的破折号数量也跟着列宽走。
# 于是「改一个单元格 = 重写整张表」：一行的语义改动产出十几行 diff，两个只改了不同行的
# 分支也必然在分隔行上撞车。实测（5 个各改一行的 PR）：两两 10 对全部冲突；去掉对齐后
# 降到 5 对，且每处只剩 1 行。
#
# prettier 没有关掉表格对齐的开关（3.8.3 实测，--help 无任何 table / align 选项），
# 忽略粒度又只到文件，所以只能整类豁免。代价是 markdown 失去列表符号 / 空行 / 转义等
# 规范化——那些是观感项，不是冲突源。
#
# 存量表格不必压成紧凑单空格：prettier 一停，改一个单元格就只动一行；表格会随编辑逐渐
# 参差，但渲染结果不变、diff 始终最小。
*.md
